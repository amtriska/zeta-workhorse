[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "zeta-workhorse"
version = "0.2.7"
description = "A calculator for graph zeta functions, spectral gaps, and tropical trace."
readme = "README.md"
authors = [
  { name="Andrew Triska", email="amtriska@gmail.com" }
]
dependencies = [
  "numpy",
  "sympy",
  "scipy",
  "networkx"
]